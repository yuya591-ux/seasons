# 範囲外メモ — 主人公を含めたキャラクターデザインの再評価（2026-07-31）

今回のフォーカス（キャラクターデザイン）の**外側**で気づいたもの。**直していない**。次回以降の検収か、別途の依頼で扱う。

## 1. `__town3dLand(true)` は窓辺からは効かない（検証用フックの限界）

- **発見箇所**: 検証用フック `__town3dLand`（`src/engine/town3dViewer.js` 10368行・`?dev=1` 限定）
- **症状**: 窓辺の状態で `__town3dLand(true)` を呼んでも地上に降りない。画面は窓辺のまま（`docs/qa/2026-07-31/hero/` の1回目の撮影で、窓辺の絵が「歩行時の絵」として撮れてしまった）
- **再現方法**: `?dev=1` で起動 → 情景を選ぶ（窓辺の状態）→ `__town3dLand(true)` → 数秒待つ → 画面は窓辺のまま。段階ボタンは「窓をあける」のまま
- **深刻度の所感**: 低（製品の不具合ではない）。ただし**これを知らずに検証すると偽の証拠を掴む**。今回1度掴んだ。
  正しくは、本物の情景メニューから情景を選び、段階ボタン（`.iconbtn--stage`）を「窓をあける → 乗り出す → 空へ → 地上へ」と4回押す。
  前回検収の `__applyScene` の罠（`docs/qa/2026-07-30_窓のむこう_キャラと賑わい_範囲外メモ.md` の2）と同種

## 2. 前回検収の未反映コミット（既知・保留中）

- **症状**: `git push origin main` が権限層で拒否され、前回検収時点で11コミットが本番未反映のまま。今回の検収でさらに増えている
- **再現方法**: `git status` / `git log origin/main..main`
- **深刻度の所感**: 中。今回の評価はすべて**ローカル現行版**に対するもので、公開版（https://yuya591-ux.github.io/seasons/ ）とは別物
