# 残タスク台帳 — 窓のむこう（2026-07-31）

**この文書が、以後の残タスクの正本。** 8系統に散らばっていた86件を、1件ずつコードで裏取りして統合した結果。

- 集めた候補 **86件**（`docs/qa/2026-07-31/backlog/raw.md`）
- 判定 **未対応51 / 対応済み10 / 不要になった2 / 判定不能23**（`backlog/verify.txt`・根拠は `backlog/evidence.txt`）
- 閉じたもの **23件**（`backlog/merge.md`）／ 統合で **29件ぶん**圧縮
- **活きている残タスク = 34件**

対象は本家 `seasons`（WebGL版）。`seasons-gpu`（WebGPU版・凍結中）は対象外。

---

## 次に着手すべき上位5件

### 1. 「窓をあける」ボタンが指を受け取るようにする（`src/style.css:860` に `z-index`）

- **なぜ今か**: 主役のボタンが押せない。`.town3d-stage` に `z-index: 0` があり、`.topbar` には指定が無いため、あとから差し込まれる3Dの面が上に来る。実測で `elementFromPoint` が CANVAS を返す（「空へ」「地上へ」は正常）
- **何が変わるか**: アプリの入口の操作が通る。**他のどの改善よりも先に効く**
- **どれくらいで終わるか**: 1行。危なさは重なり順だけ

### 2. 現代homeの住人10体の「頭の浮き」を止める（`town3dViewer.js:9092` の `1.6` → `1.11`）

- **なぜ今か**: うなずきの処理が、等身を是正した後の正しい値 1.11 ではなく**是正前の 1.6 を毎フレーム書き戻す**。戻す処理が無いので**永久に浮いたまま**。現代homeの高品質住人**10体すべて**で頭が肩から約50cm離れている
- **何が変わるか**: 窓辺から見える住人9体の破綻が消える。**窓辺は最長滞在の画面**
- **どれくらいで終わるか**: 1行

### 3. 未反映のコミットを本番へ送る

- **なぜ今か**: **5番の実機確認は公開版を見るので、送っていないと測る意味が無い**。現時点で復元地点 `105874c` が未push
- **何が変わるか**: 実機で見るものと、ここで直したものが一致する
- **どれくらいで終わるか**: `git push origin main` 1回（過去に権限層で拒否された経緯があるため、通らなければ裕也さんの手で）

### 4. 高品質住人73体のうち49体（67%）に脚と手を付ける

- **なぜ今か**: `makeResident` の `kimono`/`hakama`/`armor` 分岐が `buildLegs`(6795行) と手(6792行) を呼んでいない。**江戸20体・戦国18体・雲海7体は全員**が、振れる脚も手も無い。刀・槍も手から離れて浮く
- **何が変わるか**: 「マネキン感」の最大の原因が消える。**#285「人物を人らしく」の実体がこれ**
- **どれくらいで終わるか**: 中。衣装分岐の中で閉じるので危なさは小さい。描画コールはほぼ増えない（実測: `makeResident` は1体+6メッシュで **+45コール＝+2.6%**、上限61メッシュまで使える）

### 5. iPhone実機で1回測る（裕也さんの手作業）

- **なぜ今か**: 台帳の**下位10件のうち5件が、この結果を待って止まっている**（海岸線・284個の統合・窓アトラス・#260・グレードのシェーダー内蔵化）。机上で分かるのは描画コールまでで、発熱の体感は実機だけが答えを持っている
- **何が変わるか**: 「発熱対策をこれ以上やるべきか」が決まる。今の窓辺は **1832コール**（発熱対策前は2101）
- **どれくらいで終わるか**: 手順書は `docs/qa/2026-07-28_実機確認.md` に用意済み。輝度50%固定で12分×2本

---

## 台帳（34件・優先度順）

**並べ方**: ①1行で直る不具合を先に ②実機の答えを待つものを中ほどに ③絵を壊す危険が大きい大工事を最後に。
**依存関係が前後しないこと**を確認済み（下の「依存の確認」参照）。

「効き」＝体験がどれだけ変わるか。「危なさ」＝`R()` の決定性・既存の絵を壊す恐れ。

### 第1群: 1行〜小さな修正で直る（危なさ小・先に片づける）

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 1 | `.topbar` に `z-index` を与えて3Dの面より前に出す | C | 未対応（`style.css` 860〜872行に指定なし） | 最大 | 1行 | 小 | — | 1行消す |
| 2 | うなずきの `1.6` → `1.11`（頭の浮き） | B | 未対応（`town3dViewer.js:9092`） | 最大 | 1行 | 極小 | — | 1行戻す |
| 3 | 未反映のコミットを本番へ送る | F | 未対応（`git log origin/main..main` に残がある） | 大 | 極小 | 小 | — | — |
| 4 | `kimono`/`hakama`/`armor` に `buildLegs` と手を通す | A/B/C | 未対応（6796〜6812行に `buildLegs()` なし） | 最大 | 中 | 小 | — | 分岐ごと `if (false)` で温存 |
| 5 | iPhone実機で1回測る | A/B/D/E | 判定不能（実機のみ） | 最大 | 小（裕也さん） | — | **3** | — |
| 6 | 小物（刀・槍・杖）を `arms[i]` の子へ移す | B | 未対応（6858〜6870行に `arms[` なし） | 大 | 小 | 小 | **4** | 座標を戻す |
| 7 | 立ち絵の足元の0.21m浮きを直す | B | 未対応（`town3dViewer.js:6749` `position.y = 1.03`） | 中 | 1行 | 極小 | — | 1行戻す |
| 8 | GitHub Actions の非推奨を解消（`upload-pages-artifact@v3`） | G | 未対応（`.github/workflows/deploy.yml:33`） | 小 | 極小 | 小 | — | 版を戻す |

### 第2群: 賑わいと配置（街の広さに人が追いついていない）

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 9 | `placeEra` の配置半径を陸の縁に合わせる（`rr = 8 + R() * 22`） | C | 未対応（`town3dViewer.js:6965`） | 最大 | 小 | **中**（`R()` の消費順は不変でも座標が変わり下流の生成物がずれる） | — | 定数を戻す |
| 10 | 谷戸に人を置く（`kind !== 'yato'` のゲートを人物だけ外す） | C | 未対応（`town3dViewer.js:6894` ほか。ファイル全体で30箇所） | 大 | 中 | 小（谷戸は独立） | — | ゲートを戻す |
| 11 | 雲海の人物を島ごとに作り分けて増やす（7体 → 4島×3〜5人） | B/C | 未対応（`queueCloudFolk` は7箇所・`buildFolk` は常に `outfit:'kimono'`） | 中 | 中 | 小 | — | `buildFolk` を戻す |

### 第3群: キャラの造形（#285「人物を人らしく」の実体）

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 12 | 体格の型6種＋身長の振れ幅を±14%へ | B | 未対応（6760〜6890行に体格の型なし） | 大 | 小 | 中（`R()` の消費数を増やさない実装が必須） | **4** | 定数を1つに戻す |
| 13 | 江戸・戦国に女性・子供・老人を入れる | B | 未対応（江戸の factory 6968〜6972行は侍・行商・町人の3役のみ。戦国も足軽・武者・農夫の3役） | 大 | 中 | 中（同上） | **4** | factory を戻す |
| 14 | 髪を作り分ける（帽子の下・頭頂・髷） | B/C | 未対応（6840〜6870行に髪型の分岐なし。帽子の33体は同じ坊主頭） | 中 | 小 | 小 | — | 分岐を戻す |
| 15 | `makeResident` に歩き癖の個体差を移植（`makePeep` の6値） | B | 未対応（6870〜6880行に `gaitAmp`/`armAmp`/`cadMul` なし） | 中 | 小 | 小 | **4** | 値を固定に戻す |
| 16 | 群衆(mkCrowdPerson)の造形を直す（背丈3→8段階・肩の段差） | B/C | 未対応（背丈は呼び出し側の定数3種のみ）／肩は判定不能（撮って見る） | 中 | 小 | 中（`R()`） | — | 係数を戻す |
| 17 | 3Dの主人公 `makeHero()` を作り、立ち絵と入れ替える | B | 未対応（`makeHero` はファイル全体で0件） | 大 | 大 | 中（新規モード） | **4, 12** | 設定で非表示 |
| 18 | 中間の層を1つ足す（`makePeep` に顔テクスチャ等） | C | 未対応（6590〜6620行に顔テクスチャなし） | 小 | 中 | 中（`makePeep` は1メッシュ **+45.5コール**＝唯一の制約層） | — | 層を外す |

### 第4群: 操作と導線

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 19 | 横持ち(900×500)のUIを直す（ヘッダーのはみ出し・44px・重なり） | C | 未対応（評価が問題を見つけた2026-07-30以降、`src/style.css` は1度も変わっていない） | 大 | 中 | 小（メディアクエリ内に閉じる） | — | メディアクエリを戻す |
| 20 | 歩行カメラを近接物で自動的に寄せる（樹冠・庇・人物） | C | 未対応（カメラの近接退避の記述なし） | 中 | 小 | 小 | — | 対象リストを戻す |
| 21 | #277 歩行の迷子解消（降りたあとの行き先） | A/D | 未対応（道しるべ・行き先の案内なし。`onLocation` は現在地表示＝#189で別物） | 大 | 中 | 小 | — | 導線を外す |
| 22 | #270 行き先プレビュー＋渡しショートカット | A/D | 未対応（`src/main.js` に該当なし） | 中 | 中 | 小 | **21** | 導線を外す |
| 23 | 戦国の歩ける場所を作る（詰まり率63%） | C | 判定不能（歩いて測り直す） | 大 | 中 | 中（地形は多くの物の土台） | — | 地形パラメータを戻す |

### 第5群: 絵の破綻と監査

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 24 | 江戸の近接で地面の一枚板がカメラを割る | F | 判定不能（目線の高さで撮る） | 中 | 中 | 中 | — | 分割数を戻す |
| 25 | 樹冠に埋まる人物を取り下げる（配置監査に人物×樹冠を足す） | C | 未対応（監査に該当の判定なし） | 小 | 中 | 小 | — | 判定を外す |
| 26 | 時代エリアに降りたときのキャラ単価を測る | B | 未対応（時代エリアでの計測記録なし） | 中（判断材料） | 小 | — | — | — |

### 第6群: 音

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 27 | 薄い情景に実素材の環境音を入れる（#172＋#281） | A/D | 未対応（**25情景のうち音源フォルダがあるのは9情景・実ファイルは13本**） | 大 | 大 | 小 | — | 素材を外す |

### 第7群: 発熱・描画コール（**5番の実機確認の答えを待つ**）

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 28 | #244 二重WebGLコンテキスト解放 | A/D/E | 未対応（`src/main.js` に `loseContext` なし） | 小（**メモリ衛生のみ・発熱には効かない**） | 小 | 中 | — | 解放を外す |
| 29 | 白＋頂点色284個を束ねる（街の下敷きの残り） | D | 未対応（電線・太鼓橋・電柱は実施済みで−236。残りが284個） | 中 | 大 | 中（木のフェード・時代エリアのカリングを壊さないこと） | **5** | `?nomerge=1` |
| 30 | 窓テクスチャの共有アトラス化（−400超） | D/H | 未対応（アトラスの実装なし） | 中 | 大 | **大**（建物ごとの窓の表情が変わらないかの検証が重い） | **5** | `?nomerge=1` |
| 31 | #260 建物の脱ローポリ・観覧車の作り直し | A/D/E | 未対応（建物LODなし） | 大（発熱＋画質） | 大 | 大 | **5** | 統合を外す |
| 32 | #305 海岸線のさらなる作り込み | A/D | 判定不能（汀のディテールは `addCoastDetail`(928行) で実装済み。「さらに」の程度が未定義） | 中 | 中 | 中 | **5** | 形の合成を戻す |
| 33 | グレードのシェーダー内蔵化（Step3・発熱対策の最大レバー） | E | 未対応（`__town3dGradeMode` は revert 済みで0件） | **最大**（コンポジタの仕事が約9割減） | 大 | **最大**（全town3d情景約15枚の色が変わりうる。3度の試作で夕の一致に至らず保留） | **5** | revert |

### 第8群: 長期

| 順 | やること | 出所 | 裏取り | 効き | 手間 | 危なさ | 依存 | 戻し方 |
|---|---|---|---|---|---|---|---|---|
| 34 | #232 モノリス分割（`town3dViewer.js` 10,861行） | A/D | 未対応（単一ファイルのまま） | 小（保守性） | 大 | 大 | — | **戻しにくい** |

### 依存の確認

依存を持つ7件が、いずれも依存先より**後ろ**に置かれていることを確認した。

| 依存する側 | 依存先 | 順序 |
|---|---|---|
| 5 実機で測る | 3 本番へ送る | 3 → 5 ✓ |
| 6 小物を手へ | 4 脚と手を通す | 4 → 6 ✓ |
| 12 体格の型 / 13 配役 / 15 歩き癖 | 4 | 4 → 12,13,15 ✓ |
| 17 3Dの主人公 | 4, 12 | 4,12 → 17 ✓ |
| 22 行き先プレビュー | 21 迷子解消 | 21 → 22 ✓ |
| 29,30,31,32,33 | 5 実機で測る | 5 → 29〜33 ✓ |

---

## 閉じたもの（23件・再開しない）

詳細と根拠は `docs/qa/2026-07-31/backlog/merge.md` の「2. 死蔵の判定」。

| 分類 | 件数 | 中身 |
|---|---|---|
| **既に実装済み**（行番号で確認） | 10 | ほこりの丸い絵柄／着地の退避／音のNaN／電線57本の統合／太鼓橋51個の統合／雲の近接フェード |
| **方針として決着済み** | 6 | #279 角部屋（#259で3D載せ替え済み・`src/scenes/cornerRoom.js` は存在しない）／#276 エリア磨き（商品レベルと判断済み）／#288 キャラ=セル（方針は確定・実体は第3群） |
| **見送りが結論** | 4 | fps/解像度の降格・WebGPU移行・OffscreenCanvas+Worker・mediump化（発熱対策計画P3「判断済み・蒸し返さない」） |
| **製品の課題ではない** | 3 | `__applyScene` と `__town3dLand` の限界（検証用フック側の話・直さない）／フレーム時間の限界 |

**このうち最も重要なのは「既に実装済み」の10件**。3日前の棚卸しが「今すぐ効く」の1〜3位に置いていた3件（ほこりの四角・着地が車の中・音のNaN）は、その後の `91ee08d` `53d967d` で実装されていた。**文書の読み比べだけで整理していたら、実装済みのものを残タスクとして数え直していた。**

## 訂正した誤り

| 何 | 誤り | 正しい値 |
|---|---|---|
| タスクリストの未完了数 | 16件（pending 13・in_progress 3） | **13件（pending 10・in_progress 3）** |
| #244 の位置づけ | 2026-07-28 棚卸しで「今すぐ効く」5位（軸=発熱） | **発熱には効かない・メモリ衛生のみ**（発熱対策計画P3・#262完了により結論済み） |
| 出所Fの件数の数え方 | 見出し8件 | **7件**（「## 追記」は対処の記録で、発見ではない） |

---

## この台帳の前提（2026-07-31 実測）

| 項目 | 値 | 取り方 |
|---|---|---|
| ビルド | exit code 0 | `npm run build` |
| 全情景のスモーク | 25情景・エラー0 | `npm run smoke` |
| 窓辺の描画コール | **1832**（うちキャラ479＝26.1%） | `PORT=4890 node scripts/qa-charcost.mjs` |
| 窓から乗り出す | 1810（キャラ503＝27.8%） | 同上 |
| 低空を飛ぶ | 1687（キャラ452＝26.8%） | 同上 |
| 地上を歩く | 1694（キャラ469＝27.7%） | 同上 |
| キャラ1体+6メッシュの実費 | `makeResident` +45 ／ `makePeep` **+273** ／ 群衆 −5 ／ 旅人 +9 ／ 立ち絵 +17 | 同上 |

**発熱対策の前後**: 窓辺 2101（対策前）→ **1832**（現在）。
**抑えるべき層は `makePeep` の1つだけ**（1メッシュ +45.5コール）。他4層は作り直してよい。

---

## 古い文書の扱い

以下は**参照用の記録**として残すが、残タスクの正本ではない。**残タスクを確認するときは、この台帳だけを見る。**

| 文書 | 扱い |
|---|---|
| `docs/qa/2026-07-28_残タスク棚卸し.md` | **正本から降ろす**（冒頭に統合済みの旨を追記）。3件が実装済みで古い。ただし描画コールの内訳の分析（424種類の長い裾・窓460個のテクスチャ）は今も有効な材料 |
| `docs/qa/2026-07-28_全体評価.md` | 参照。優先度つきアクション6件はこの台帳に統合済み |
| `docs/qa/2026-07-30_キャラと賑わいの評価.md` | 参照。12件はこの台帳に統合済み。根拠の数値（無人地帯24〜36m・詰まり率63%）は今も有効 |
| `docs/qa/2026-07-31_キャラクターデザインの評価.md` | 参照。12件はこの台帳に統合済み。**層別の性能予算と上限の表は今も判断材料として有効** |
| `発熱対策計画_2026-07.md` | **発熱については引き続きこれが正本**。Step1〜5の実施記録と、3度の試作で得た知見（atmo層順・premul補間・washの高彩度soft-lightズレ）が残っている |
| 範囲外メモ3本 | 参照。全件この台帳に統合済み |
