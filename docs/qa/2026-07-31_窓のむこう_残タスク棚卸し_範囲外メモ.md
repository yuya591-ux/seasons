# 範囲外メモ — 残タスクの全数棚卸し（2026-07-31）

今回のフォーカス（残タスクを集めて裏取りして台帳にする）の**外側**で気づいたもの。**直していない**。

## 1. `kind !== 'yato'` のゲートが30箇所に散らばっている

- **発見箇所**: `src/engine/town3dViewer.js`（全体で30箇所）
- **症状**: 谷戸を人物・什器などから除外するゲートが、1つのフラグではなく**同じ条件式の30回のコピー**で書かれている。台帳の「谷戸に人を置く」（第2群10番）を実施するとき、人物のブロックだけを正確に選び出す必要があり、取りこぼすと直したつもりで直っていない状態になる
- **再現方法**: `grep -c "kind !== 'yato'" src/engine/town3dViewer.js` → `30`
- **深刻度の所感**: 中。製品の不具合ではないが、**着手時の事故の元**。実施の前に30箇所を分類する調査が要る（2026-07-30の評価は人物ブロックとして 6894/6899/6917/6934/6947 の5箇所を挙げているが、それが全部かは未確認）

## 2. タスクリストに #126 以前の項目が1つも残っていない

- **発見箇所**: セッションのタスクリスト（#127〜#315）
- **症状**: 番号は #127 から始まっており、#1〜#126 が存在しない。過去に整理で消されたと見られるが、**その中に未完了のものが混ざっていたかどうかを確認する術が無い**
- **再現方法**: タスクリストの先頭を見る
- **深刻度の所感**: 低〜中。今回の棚卸しは #127 以降と6系統の文書を突き合わせているので、実質的な取りこぼしは小さいと見ているが、**「全数」と言い切れない唯一の穴**。台帳にはこの限界を明記していない（このメモが記録）

## 3. `qa-backlog.mjs` のプローブは**書いた本人の思い込みを検出できない**

- **発見箇所**: 今回作った `scripts/qa-backlog.mjs`
- **症状**: 初回の実行で **4件の誤判定**が出た（`迷子` が現在地表示に、`角部屋` が壁のコメントに、`outfit: 'dress'` が大正のモダンガールに、`太鼓橋` が公園の説明文に当たった）。いずれも「正規表現が当たった＝対応済み」と出るため、**結果だけ見ていたら気づかない**
- **再現方法**: 正規表現を広く書いて実行し、ヒットした行を実際に読まずに信じる
- **深刻度の所感**: 中。**このスクリプトを次に使うときは、「対応済み」と出た全件の根拠行を必ず1つずつ読むこと**（`--evidence` はそのために作った）。今回は10件すべて読んで確認した
