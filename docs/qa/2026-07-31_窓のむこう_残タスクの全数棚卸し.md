# 検収計画 — 窓のむこう: 残タスクの全数棚卸しと整理（2026-07-31）

対象: 本家 `seasons`（WebGL版）ローカル現行版 ＝ `e37826c`（本番反映済み・未反映0）
主眼: **「まだ残っているもの」を出所ごとに漏れなく集め、1件ずつコードで裏取りして、優先度つきの単一の台帳にまとめる**。
**今回は実装しない**（承認記録1）。

---

## 調査で分かったこと（表を作る前に実物を確認した）

### 残タスクの出所は6系統に散らばっている

| 出所 | 場所 | 件数（暫定） | 状態 |
|---|---|---|---|
| A. タスクリスト | セッション内（ディスクに無い） | **未完了16件**（pending 13・in_progress 3） | 番号だけで中身が分からないものが多い |
| B. キャラクターデザインの評価 | `docs/qa/2026-07-31_キャラクターデザインの評価.md` | **12件**（優先度つきアクション） | 本日作成。未着手 |
| C. キャラと賑わいの評価 | `docs/qa/2026-07-30_キャラと賑わいの評価.md` | **12件**（優先度つきアクション） | 昨日作成。未着手 |
| D. 全体評価・残タスク棚卸し | `docs/qa/2026-07-28_全体評価.md` / `2026-07-28_残タスク棚卸し.md` | 全体評価6件＋棚卸し16件 | **3日前。既に上位3件は実装済み＝古い** |
| E. 発熱対策計画 | `発熱対策計画_2026-07.md`（リポジトリ直下・正本） | Step3保留・P3見送り・#260ほか | Step1/2/4/5は完了。Step3は3度の試作で保留 |
| F. 範囲外メモ | `docs/qa/2026-07-{28,30,31}_*_範囲外メモ.md` 3本 | 各1〜3件 | 検収の外で見つけたもの。手つかず |

**加えて、どの文書にも載っていない未処理が1件**: GitHub Actions の `actions/upload-artifact@v4` が Node.js 20 を指定しており、Node 24 で強制実行される非推奨警告（本日のデプロイログで確認）。

### 3日前の棚卸しは既に古い — だから「文書の突き合わせ」では足りない

`docs/qa/2026-07-28_残タスク棚卸し.md` の「今すぐ効く」上位3件（ほこりの四角・着地が車の中・音のNaN）は、
その後のコミット `91ee08d` `53d967d` で**すでに実装済み**。文書だけを読み比べると、**実装済みのものを残タスクとして数え直す**ことになる。

→ 承認記録3のとおり、**全件をコードで1件ずつ裏取りする**。判定は「未対応 / 対応済み / 不要になった / 判定不能（人の目が要る）」の4値。

### 使える道具（既存・確認済み）

`npm run smoke`（`scripts/qa-smoke.mjs`・全情景のスモークテスト）／`npm run walk`（`scripts/qa-debug-walk.mjs`・全情景の自動巡回）／
`npm run build`／今回までに作った検証スクリプト11本（`scripts/qa-*.mjs`）。

---

## 計画表

### 第1部: 材料を漏れなく集める

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 1 | 出所A〜F＋インフラの**全候補を1件も落とさずに書き出す**（出所・原文・そのままの表現を保持） | `docs/qa/2026-07-31/backlog/raw.md`（新規）＋ `grep -c '^| ' docs/qa/2026-07-31/backlog/raw.md` の出力（総件数） | [ ] | |
| 2 | **抽出漏れの検査**。各出所の原文の項目数を数え、raw.md に載った数と突き合わせる（差が出たら理由を書く） | `PORT=4890 node scripts/qa-backlog.mjs --count`（新規）の出力＝出所ごとの「原文の件数 / 台帳に載った件数 / 差」の表 | [ ] | |

### 第2部: 1件ずつコードで裏取りする

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 3 | 裏取りスクリプトを作る。**各項目に「コード上のどこを見れば判定できるか」のプローブを1つずつ与える**（grep のパターン・ファイルの有無・数値の範囲） | `node scripts/qa-backlog.mjs --list`（新規）で全項目とプローブが一覧表示され、exit code 0 | [ ] | |
| 4 | **全件の判定を実行**する（未対応 / 対応済み / 不要になった / 判定不能） | `node scripts/qa-backlog.mjs` の出力全文 ＋ `docs/qa/2026-07-31/backlog/verify.txt`。末尾に4値の内訳 | [ ] | |
| 5 | **「対応済み」と判定された項目**について、そう言える根拠の行（ファイル:行番号と該当ソース）を残す | 同スクリプトの `--evidence` 出力。1件につき「判定・根拠ファイル:行・該当行の中身」 | [ ] | |
| 6 | **「判定不能」の項目**を明示し、なぜプローブが書けないか（人の目・実機・主観が要る）を1件ずつ書く | `docs/qa/2026-07-31/backlog/undecidable.md`（新規）。1件につき「なぜ機械で判定できないか」「代わりに何を見れば決まるか」 | [ ] | |

### 第3部: 台帳の前提を測り直す

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 7 | **今のアプリが壊れていないこと**を確認する（台帳の前提。ここが崩れていたら優先度が変わる） | `npm run build`（exit code 0）＋ `npm run smoke`（全情景・エラー0）の生ログ | [ ] | |
| 8 | **性能の現在値**を取り直す（3日前の数字が台帳の前提になっているため） | `PORT=4890 node scripts/qa-charcost.mjs` の #10 の表（窓辺／乗り出す／低空／歩行の総描画コール）＋ 前回値との差 | [ ] | |

### 第4部: 整理して1本の台帳にする

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 9 | **重複・同一実体の統合**（別の名前で同じことを言っている項目をまとめる。例: #285「人物を人らしく」と B群のキャラ改善） | `docs/qa/2026-07-31/backlog/merge.md`（新規）＝「統合前の件名 → 統合後の1件」の対応表と、統合で減った件数 | [ ] | |
| 10 | **死蔵の判定**（もう不要・方針転換で消えた項目を、理由つきで落とす） | 同ファイルの後半＝「落とす件名・理由・落として良いと判断した根拠」 | [ ] | |
| 11 | **統合台帳を作る**。1件ごとに「出所／裏取り結果／効き／手間／危なさ／依存関係／戻し方」を持つ単一の表 | `docs/qa/2026-07-31_残タスク台帳.md`（新規・以後これを正本にする） | [ ] | |
| 12 | 台帳を**優先度で並べ替え**、上位から着手できる形にする（依存関係が前後しないこと） | 同ファイルの並び順＋「なぜこの順か」の1行説明。依存の前後関係が壊れていないことの確認出力 | [ ] | |

### 第5部: 次の一手

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 13 | **次に着手すべき上位5件**を、根拠つきで推奨する（1件ずつ「なぜ今か」「何が変わるか」「どれくらいで終わるか」） | `docs/qa/2026-07-31_残タスク台帳.md` の冒頭の節 | [ ] | |
| 14 | 古くなった文書の**扱いを決める**（`2026-07-28_残タスク棚卸し.md` を正本から降ろし、参照に回す旨を明記） | 同台帳の末尾＋ `2026-07-28_残タスク棚卸し.md` の冒頭に「この文書は 2026-07-31 の台帳に統合された」の1行を追記した差分 | [ ] | |

---

## 承認記録（2026-07-31・裕也さん）

1. **成果物は棚卸し台帳まで。実装はしない**（最優先の実装も、計測の穴埋めも今回はしない）
2. **対象はアプリ全体**（タスクリストの未完了・評価文書の未対応・範囲外メモ・発熱対策計画の残り・今回のキャラ評価12件をすべて統合する）
3. **裏取りはコードで1件ずつ**（文書の突き合わせだけで済ませない。3日前の棚卸しが既に3件古くなっているため）

---

## 対象外（今回は触らない）

- **実装・修正のすべて**（承認記録1。裏取りで「未対応」と出ても直さない）
- **未測定の計測の穴埋め**（時代エリアでのキャラ単価・実機での発熱確認。台帳には「未測定」として1件立てるが、今回は測らない）
- **WebGPU版 `seasons-gpu`**（凍結のまま。棚卸しの対象外＝承認記録2で「アプリ全体」を選ばれたが、本家のみを指すものとして扱う）
- **タスクリストそのものの削除・整理**（台帳を作るだけ。リスト側の一括整理は別途）
