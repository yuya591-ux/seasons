# 検収計画 — 窓のむこう: 発熱抑制の本丸と全体再点検（2026-07-28）

対象: 本家 `seasons`（WebGL版 / https://yuya591-ux.github.io/seasons/ ）
主眼: **画質・クオリティ・操作の快適さを維持したまま、iPhoneの発熱だけを下げる**。あわせて残タスクの棚卸しとアプリ全体評価。

## 調査で分かったこと（この計画の根拠・すべて実測）

実測1: 全状態の負荷（`scripts/qa-perf7.mjs`・2026-07-28）

| 状態 | 描画コール | 三角形 | JS時間 |
|---|---|---|---|
| **窓辺（既定・最長滞在）** | **2135** | 731k | 1.08ms |
| home低空飛行 | 1851 | 675k | 1.72ms |
| home歩行 | 1268 | 574k | 1.26ms |
| 雲海(y100) | 1364 | 251k | 1.16ms |
| 江戸/大正/戦国 低空 | 207〜451 | 243〜283k | 1.0ms前後 |

実測2: iPhone相当条件で実操作（実GPU＋CPU4倍スロットル・393×852・DSF3）

| 状態 | 描画コール | 実効解像度 | 描画fps | 最悪間隔 | カクつき率 |
|---|---|---|---|---|---|
| 窓辺 | 2063 | **1.48**（上限1.6から自動降格） | 9.4 | 126ms | 0% |
| 低空飛行 | 1713 | 1.48 | 8.6 | 137ms | 0% |
| 歩行 | 1265 | **1.24** | 10 | 210ms | 2.2% |

実測3: 窓辺2138コールの内訳（`scripts/qa-perf8.mjs`）

- 街の建物 `townChildren` **1646**（最大の塊）／住民194／輪郭161／公園136／室内125／観覧車112／木80／寺63
- そのうち**「重い32棟」だけで809メッシュ**＝全体の約38%。上位は1棟で74・53・51・50メッシュ
- 一方、既に1メッシュに統合済みの建物が1087棟（＝易しい統合は完了済み）

**結論（発熱の主因）**: JS時間は全状態1ms台で健全。三角形数も節度あり。残る主因は
**①描画コール2000超のCPU提出コスト**（iOSはANGLE→Metal変換で1コールが高い＝正本docの筆頭項）と
**②CSS合成5層のコンポジタ仕事**（ネイティブ解像度で約12MP/フレーム＝正本docの最重大項）。
CPU4倍条件で解像度が自動的に1.6→1.48→1.24へ落ちている事実は、**発熱と「画質が悪い」が同じ根**（負荷過多→自動品質が解像度を譲る）であることを示す。

**方針**: 描画コール削減は**絵を1ミリも変えずに**負荷を下げる唯一の大レバー（同じ形・同じ色を、少ない回数で送るだけ）。ここを本丸に据える。CSS層とDPRは絵が変わるため、承認を得た分だけ行う。

### この計測の限界（正直に）

デスクトップGPUは桁違いに速いため、**画素側（CSS合成・解像度・ブルーム）のGPU負荷はfpsに現れない**。そこは解析モデル＋実機A/Bで判断する。最終判定は実機1回（第5部）。

---

## 計画表

### 第1部: 発熱の本丸 — 絵を変えずに描画コールを減らす（#260）

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 1 | 重い32棟の正体（構造物名・材質構成・統合可能なグループ）を特定する調査フックを追加 | `PORT=4890 node scripts/qa-heavy.mjs`（上位20件の名前・メッシュ数・材質数を出力） | [ ] | |
| 2 | 窓辺に効く建物を材質別にグローバル統合（1変更=1コミット・R()消費順を壊さない後処理パス方式） | `PORT=4890 node scripts/qa-perf8.mjs`（base 2138 → **1400以下**） | [ ] | |
| 3 | 統合の前後で絵が変わっていないことを画素差で証明（窓辺・昼／夜／夕／雪の4条件） | `node scripts/qa-imgdiff.mjs`（平均差がアニメ床値以下） | [ ] | |
| 4 | 統合後も全情景が壊れていないこと | `npm run smoke`（25情景・エラー0） | [ ] | |
| 5 | 全情景を自動操作して配置違反・黒画面・描画コール急増が無いこと | `npm run walk`（フラグ0） | [ ] | |
| 6 | iPhone相当条件で改善を再計測（前後比較） | `HEADED=1 PORT=4890 node scripts/qa-gate-baseline.mjs`（窓辺の描画fps・カクつき率・実効解像度） | [ ] | |

### 第2部: 発熱の第2レバー — わずかに絵が変わる（2026-07-28 承認済み）

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 7 | CSS合成層を5→3へ（粗い斑 `paper2`・縁のにじみ `bleed` を畳む／`?fullcss=1` で旧5層を完全復元できる退避つき） | `PORT=4890 node scripts/qa-cssab.mjs`（同一ページ内トグルの画素差%・昼/夜/夕/雪） | [ ] | |
| 8 | 飛行中だけ解像度帯を1.2に固定（窓辺の静止は1.6のまま＝眺める時の鮮明さは不変） | `PORT=4890 node scripts/qa-dprband.mjs`（窓辺1.6→飛行1.2→帰還1.6の遷移ログ） | [ ] | |

### 第3部: 実操作で見つかった要確認（第1部の作業前に事実確認する）

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 9 | 着地直後の構図: 足元の灰色が画面下1/3を占め、近景の木がにじむ（着地アニメ中の一時的なものか、定常かを切り分け） | スクショ `docs/qa/2026-07-28/09_着地_直後.png` / `09_着地_安定後.png` | [ ] | |
| 10 | 雲海高度で近接の雲が白い塊で画面を覆う（#289の近接フェードが効いているかの確認） | スクショ `docs/qa/2026-07-28/10_雲海_近接.png` ＋ `__town3dFlyPose` 3地点 | [ ] | |
| 11 | 窓辺のほこりが硬い白い四角に見える（DSF3での粒の描画品質） | スクショ `docs/qa/2026-07-28/11_窓辺_ほこり拡大.png` | [ ] | |

### 第4部: 棚卸しと評価

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 12 | 残タスク全件の棚卸し（未着手10件＋進行中3件）を、発熱・画質・体験の軸で優先順位づけ | `docs/qa/2026-07-28_残タスク棚卸し.md`（全件に理由と推奨順位） | [ ] | |
| 13 | アプリ全体評価（総評／強み／弱点／優先度付きアクションの4ブロック・弱点はファイル名と数値つき） | `docs/qa/2026-07-28_全体評価.md` ＋ 根拠スクショ6枚以上 | [ ] | |

### 第5部: 締め

| # | 項目 | 完了の証拠 | 状態 | 実出力 |
|---|---|---|---|---|
| 14 | ビルドが通り、本番へ反映される | `npx vite build`（exit code 0）＋ GitHub Actions の成功ログ | [ ] | |
| 15 | 実機確認は1回だけ。同条件ABの手順書を渡す | `docs/qa/2026-07-28_実機確認.md`（輝度50%固定・窓辺5分／飛行5分・記録項目） | [ ] | |

---

## 承認記録（2026-07-28・裕也さん）

1. **土台は本家WebGL版に一本化**。WebGPU版 `seasons-gpu` は凍結（消さずに残す）。実機で3Dが真っ黒／手元で再現できないため、直せる見込みが立たないと判断
2. **CSS合成層は5→3へ減らす。ただし `?fullcss=1` で旧5層に完全復元できる退避スイッチを必ず付ける**（実機で見比べて気に入らなければ即戻せる形）
3. **第3部の3点は、直す前に事実確認（スクショ切り分け）から**。着地アニメ中の一瞬か常時かで対応が変わるため、切り分け結果を報告してから修正可否を決める

---

## 対象外（今回は触らない）

- WebGPU版 `seasons-gpu`（実機で3Dが真っ黒。凍結し、本家に注力する＝質問1）
- #232 モノリス分割・#288 キャラのセルルック・#285 人物の作り直し（発熱と無関係の大工事）
- #260のうち観覧車の造形作り直し（描画コール統合とは別作業。第4部で優先順位づけのみ）
